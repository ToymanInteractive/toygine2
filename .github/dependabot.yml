version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"

    assignees:
      - "dkrutskikh"

    commit-message:
      prefix: "chore(ci): "

    groups:
      github-actions:
        patterns:
          - "actions/*"

      docker-actions:
        patterns:
          - "docker/*"
