# Руководство по стилю документации Doxygen

Этот документ описывает полное руководство по стилю для Doxygen документации, используемой во всем проекте.

## Содержание

1. [Общее форматирование](#общее-форматирование)
2. [Документация файлов](#документация-файлов)
3. [Документация классов/структур](#документация-классовструктур)
4. [Документация методов/функций](#документация-методовфункций)
5. [Документация параметров](#документация-параметров)
6. [Документация возвращаемых значений](#документация-возвращаемых-значений)
7. [Предусловия и постусловия](#предусловия-и-постусловия)
8. [Заметки и предупреждения](#заметки-и-предупреждения)
9. [Перекрестные ссылки](#перекрестные-ссылки)
10. [Требования к типам](#требования-к-типам)
11. [Константы и значения](#константы-и-значения)
12. [Примеры кода](#примеры-кода)
13. [Разделы](#разделы)

---

## Общее форматирование

### Блоки комментариев

- Используйте `/*! ... */` для многострочных блоков документации
- Используйте `///` для однострочных комментариев (обычно для переменных-членов)
- Всегда начинайте с `/*!` на новой строке
- Выравнивайте закрывающий `*/` с открывающим `/*!`

```cpp
/*!
  \brief Краткое описание.

  Подробное описание здесь.
*/
```

### Отступы

- Используйте 2 пробела для отступов в блоках документации
- Выравнивайте команды Doxygen (например, `\param`, `\return`) в один столбец
- Поддерживайте единообразные отступы между разделами

---

## Документация файлов

Каждый заголовочный файл должен начинаться с блока документации файла:

```cpp
/*!
  \file   filename.hpp
  \brief  Краткое описание назначения файла.
*/
```

**Правила:**
- Используйте `\file` с фактическим именем файла (относительно директории include)
- `\brief` должен быть кратким однострочным описанием
- Размещайте перед include guards

---

## Документация классов/структур

### Структура

Документация класса должна следовать этой структуре:

1. `\class` или `\struct`
2. `\brief` - однострочное описание
3. Подробное описание (2-4 абзаца)
4. `\tparam` - параметры шаблона (если применимо)
5. `\section features Key Features` - список с эмодзи-префиксами
6. `\section usage Usage Example` - блок кода
7. `\section performance Performance Characteristics` - информация о сложности и памяти
8. `\section safety Safety Guarantees` - информация о безопасности
9. `\section compatibility Compatibility` - требования к платформе/стандартам
10. `\note` - важные заметки (одна строка для простых заметок, несколько для сложных случаев)
11. `\warning` - предупреждения (если необходимо)
12. `\see` - связанные классы/типы

### Пример

```cpp
/*!
  \class FixedString
  \brief Класс строки-шаблона с фиксированным буфером символов.

  FixedString - это легковесный высокопроизводительный класс строки, который использует фиксированный буфер
  символов, выделенный в стеке. Он предоставляет интерфейс, подобный std::string, избегая динамического
  выделения памяти, что делает его подходящим для встроенных систем, приложений реального времени и
  критичного к производительности кода, где накладные расходы на выделение памяти должны быть минимизированы.

  \tparam allocatedSize Размер внутреннего буфера символов, включая место для нулевого терминатора.
                        Должен быть больше нуля.

  \section features Key Features

  - ⚙️ **Zero Dynamic Allocation**: Вся память выделяется в стеке во время компиляции
  - 🔧 **constexpr Support**: Большинство операций может быть вычислено во время компиляции
  - 🛡️ **Exception Safety**: Все операции являются noexcept

  \section usage Usage Example

  \code
  #include "fixed_string.hpp"
  toy::FixedString<32> str("Hello, World!");
  \endcode

  \section performance Performance Characteristics

  - ⚙️ **Construction**: O(n), где n - длина исходной строки
  - 💾 **Memory Usage**: Фиксировано во время компиляции, без выделения в куче

  \section safety Safety Guarantees

  - 🛡️ **Contracts & Debug Checks**: Границы/емкость проверяются в debug режиме
  - ⚠️ **Exception Safety**: Все операции являются noexcept, исключения не выбрасываются

  \section compatibility Compatibility

  - 🆕 **C++23**: Требуется C++23 или более новая версия для полной функциональности
  - 🌐 **Cross-Platform**: Работает на всех платформах, поддерживаемых компилятором

  \note Размер внутреннего буфера - allocatedSize, но максимальная длина строки - allocatedSize - 1 (нулевой терминатор).

  \see std::string
  \see StringLike
*/
```

### Использование эмодзи в Features

Используйте соответствующие эмодзи для категорий функций:
- ⚙️ - Управление памятью/ресурсами
- 🔧 - Языковые возможности (constexpr, noexcept и т.д.)
- 🛡️ - Гарантии безопасности/исключений
- 🔗 - Совместимость/Интеграция
- 🌍 - Интернационализация/Стандарты
- 🧬 - Безопасность типов/Концепты
- 📏 - Размер/Макет
- 🎯 - Назначение/Оптимизация

---

## Документация методов/функций

### Структура

Документация метода должна следовать этому порядку:

1. `\brief` - однострочное описание
2. Подробное описание (1-2 абзаца)
3. `\tparam` - параметры шаблона (если применимо)
4. `\param` - параметры (выровненные)
5. `\return` - возвращаемое значение
6. `\pre` - предусловия
7. `\post` - постусловия (для методов, изменяющих состояние)
8. `\note` - дополнительные заметки
9. `\see` - связанные методы/операторы
10. `\warning` - предупреждения (если необходимо)

### Пример

```cpp
/*!
  \brief Добавляет объект StringLike в конец этой строки.

  Этот метод добавляет содержимое объекта StringLike в конец этой строки. Операция добавления
  увеличивает размер строки соответственно.

  \tparam stringType Тип исходной строки. Должен удовлетворять концепту StringLike.

  \param string Исходный объект StringLike, из которого добавляется содержимое.

  \return Ссылка на эту строку после добавления.

  \pre Комбинированная длина после добавления не должна превышать выделенный размер.

  \post Объект StringLike добавлен в конец этой строки.
  \post Размер строки увеличен на размер добавленного объекта StringLike.

  \note Этот метод эквивалентен оператору присваивания сложения.

  \see operator+=(const stringType &)
*/
```

---

## Документация параметров

### Формат

- Начинайте с `\param`
- Используйте `\a` для ссылки на имя параметра в описаниях
- Выравнивайте описания параметров по самому длинному имени параметра
- Указывайте значения по умолчанию, когда применимо
- Держите описания краткими (одна строка, когда возможно)

### Выравнивание

Когда существует несколько параметров, выравнивайте описания:

```cpp
\param dest     Указатель на буфер назначения, где будут храниться данные.
\param destSize Размер буфера назначения в байтах.
\param src      Указатель на исходные данные для копирования.
```

### Значения по умолчанию

Всегда упоминайте значения по умолчанию в описаниях параметров:

```cpp
\param count     Количество раз для повтора \a character (по умолчанию: \c 1).
\param position Позиция для начала поиска (по умолчанию: \c 0).
\param tolerance Допуск для сравнения на равенство (по умолчанию: \c 0).
```

### Ограничения

- **НЕ** включайте условия или требования в описания `\param`
- Переносите все условия в теги `\pre`
- Держите `\param` сфокусированным на том, что представляет параметр, а не на его ограничениях

**Правильно:**
```cpp
\param string Исходная C строка, из которой копируется содержимое.

\pre Исходная C \a string не должна быть null.
\pre Исходная C \a string должна быть null-терминированной.
```

**Неправильно:**
```cpp
\param string Исходная C строка, из которой копируется содержимое (не должна быть null, должна быть null-терминированной).
```

---

## Документация возвращаемых значений

### Формат

- Начинайте с `\return`
- Описывайте, что возвращается
- Используйте `\c` для констант и литеральных значений
- Упоминайте специальные возвращаемые значения (например, `\ref npos`, `nullptr`)

### Пример

```cpp
\return Ссылка на эту строку после добавления.

\return \c true, если строка пуста (размер \c 0), \c false в противном случае.

\return Позиция первого вхождения \a string, или \ref npos, если не найдено.

\return Указатель на буфер назначения, или \c nullptr, если операция не удалась.
```

---

## Предусловия и постусловия

### Предусловия (\pre)

- Документируйте все требования, которые должны быть выполнены перед вызовом функции
- Включайте проверки валидности параметров
- Включайте проверки размера/границ
- Включайте требования к типам (когда не очевидно из сигнатуры)

### Формат

```cpp
\pre \a index должен быть в пределах текущей строки.
\pre Комбинированная длина после добавления не должна превышать выделенный размер.
\pre Исходная C \a string не должна быть null.
\pre Исходная C \a string должна быть null-терминированной.
\pre Итераторы \a first и \a last должны быть валидными.
```

### Постусловия (\post)

- Требуются для всех методов, которые изменяют состояние объекта
- Описывайте состояние объекта после завершения метода
- Включайте изменения размера/состояния
- Несколько тегов `\post` для нескольких изменений состояния

### Формат

```cpp
\post Эта строка содержит то же содержимое, что и исходный объект StringLike.
\post Размер строки равен размеру исходного объекта StringLike.

\post Вектор пуст (размер \c 0).
\post Все элементы правильно уничтожены.
```

---

## Заметки и предупреждения

### Заметки (\note)

- Используйте для дополнительной информации, уточнений или деталей реализации
- **Простые заметки**: Одна строка на `\note`
- **Сложные заметки**: Несколько тегов `\note` для разных аспектов
- Держите заметки краткими; длинные объяснения должны быть разделены на несколько заметок

**Простые:**
```cpp
\note Само-присваивание обрабатывается корректно и безопасно.
\note Поиск чувствителен к регистру.
```

**Сложные:**
```cpp
\note Для ASCII строк utf8_size() равно size().
\note Для UTF-8 строк utf8_size() может быть меньше size().
\note Некорректные UTF-8 последовательности обрабатываются корректно и могут влиять на подсчет.
\note Этот метод полезен для интернационализации и обработки текста.
```

- Если заметка слишком длинная для одной строки, либо:
  1. Разделите на две более короткие `\note`, или
  2. Сократите формулировку

### Предупреждения (\warning)

- Используйте для критической информации о неправильном использовании или потенциальных проблемах
- Обычно многострочные

```cpp
\warning Исходная C строка должна оставаться валидной в течение времени жизни объекта CStringView.
         CStringView не владеет данными строки и не будет управлять их временем жизни.
```

---

## Перекрестные ссылки

### Теги \see

- Добавляйте теги `\see` для всех связанных методов, классов и типов
- Связывайте между:
  - Методами и их эквивалентными операторами (`append()` ↔ `operator+=()`)
  - Константными и неконстантными перегрузками (`data()` ↔ `data() const`)
  - Синонимами методов (`size()` ↔ `length()`, `data()` ↔ `c_str()`)
  - Связанными классами (`Point` ↔ `Vector2D`, `FixedString` ↔ `CStringView`)
  - Похожими методами (`find()` ↔ `rfind()`, `find_first_of()` ↔ `find_first_not_of()`)

### Формат

```cpp
\see operator+=(const stringType &)
\see size()
\see data() const
\see Point
\see find()
```

---

## Требования к типам

### Параметры шаблона (\tparam)

- Всегда документируйте параметры шаблона
- Включайте ограничения типов и требования
- Используйте четкие, описательные имена

### Формат

```cpp
\tparam allocatedSize Размер внутреннего буфера символов, включая место для нулевого терминатора.
                      Должен быть больше нуля.

\tparam stringType Тип исходной строки. Должен удовлетворять концепту StringLike.

\tparam type          Тип элементов, хранящихся в векторе. Должен быть копируемо-конструируемым и уничтожаемым типом.
\tparam allocatedSize Максимальное количество элементов, которые могут быть сохранены в векторе. Должно быть больше нуля.

\tparam InputIterator Тип входного итератора. Должен удовлетворять концепту InputIterator и не должен быть целочисленным типом.
```

---

## Константы и значения

### Использование \c

- Оборачивайте все константы, литеральные значения и числовые значения в `\c`
- Используйте в:
  - Описаниях `\param` (значения по умолчанию)
  - Описаниях `\return`
  - Описаниях `\post`
  - Описаниях `\note`

### Примеры

```cpp
\param count Количество раз для повтора \a character (по умолчанию: \c 1).
\return \c true, если строка пуста (размер \c 0), \c false в противном случае.
\post Размер строки увеличен на \c 1.
\note Если \a count равен \c npos, все символы от \a offset до конца удаляются.
```

### Использование \a

- Используйте `\a` для ссылки на имена параметров в описаниях
- Помогает создавать читаемую, связанную документацию

```cpp
\param string Исходный объект StringLike, из которого добавляется содержимое.

Этот метод добавляет содержимое \a string в конец этой строки.
```

### Использование \ref

- Используйте `\ref` для перекрестных ссылок на другие документированные сущности
- Используйте для специальных констант (например, `\ref npos`)

```cpp
\return Позиция первого вхождения, или \ref npos, если не найдено.
\see operator+=(const stringType &)
```

---

## Примеры кода

### Примеры использования в классах

Используйте `\section usage Usage Example` с `\code ... \endcode`:

```cpp
\section usage Usage Example

\code
#include "fixed_string.hpp"

toy::FixedString<32> str("Hello, World!");
str += " This is a test.";
\endcode
```

### Встроенные ссылки на код

Используйте обратные кавычки для встроенного кода в заметках:

```cpp
\note Этот метод эквивалентен выражению: ```size() == \c 0```.
```

---

## Разделы

### Стандартные разделы

Классы должны включать эти стандартные разделы:

1. **\section features Key Features** - Список основных функций с эмодзи-префиксами
2. **\section usage Usage Example** - Пример кода, показывающий типичное использование
3. **\section performance Performance Characteristics** - Информация о сложности и памяти
4. **\section safety Safety Guarantees** - Гарантии, связанные с безопасностью
5. **\section compatibility Compatibility** - Требования к платформе/стандартам

### Формат раздела

```cpp
\section features Key Features

- ⚙️ **Название функции**: Описание
- 🔧 **Название функции**: Описание

\section usage Usage Example

\code
// Пример кода здесь
\endcode

\section performance Performance Characteristics

- ⚙️ **Операция**: Сложность или описание
- 💾 **Memory Usage**: Описание

\section safety Safety Guarantees

- 🛡️ **Категория**: Описание
- ⚠️ **Категория**: Описание

\section compatibility Compatibility

- 🆕 **Платформа/Стандарт**: Описание
- 🌐 **Платформа**: Описание
```

---

## Чеклист для документации

Перед коммитом убедитесь:

- [ ] Файл имеет блок документации `\file`
- [ ] Класс/структура имеет `\class`/`\struct` и `\brief`
- [ ] Все методы имеют `\brief`
- [ ] Все параметры имеют `\param` с правильным выравниванием
- [ ] Значения по умолчанию упомянуты в `\param`
- [ ] Все условия находятся в `\pre`, а не в `\param`
- [ ] Все методы, изменяющие состояние, имеют `\post`
- [ ] Все константы обернуты в `\c`
- [ ] Имена параметров ссылаются с помощью `\a`
- [ ] `\return` документирован для всех не-void функций
- [ ] `\tparam` документирован для всех параметров шаблона
- [ ] Теги `\note` по одной строке каждый (или правильно разделены)
- [ ] Связанные методы/классы имеют теги `\see`
- [ ] Параметры шаблона имеют документированные требования к типам

---

## Правила единообразия стиля

### Порядок документации методов

Всегда следуйте этому порядку:

```
\brief
[подробное описание]
\tparam (если шаблон)
\param (выровненные, со значениями по умолчанию)
\return
\pre
\post (для методов, изменяющих состояние)
\note
\see
\warning (если необходимо)
```

### Порядок документации классов

Всегда следуйте этому порядку:

```
\class или \struct
\brief
[подробное описание]
\tparam (если шаблон)
\section features
\section usage
\section performance
\section safety
\section compatibility
\note
\warning (если необходимо)
\see
```

### Проверки единообразия

- Используйте единообразную терминологию во всем
- Используйте единообразное форматирование для похожих конструкций
- Поддерживайте единообразный уровень детализации
- Используйте единообразные сокращения (например, "C строка" vs "C-string")

---

Это руководство по стилю должно соблюдаться для всей новой документации и использоваться как справочник при обновлении существующей документации.
